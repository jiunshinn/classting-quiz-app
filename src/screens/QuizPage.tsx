import React, {useEffect} from 'react';
import {View, Text} from 'react-native';

const QuizPage = () => {
  useEffect(() => {
    // 퀴즈 데이터 로드
  }, []);

  return (
    <View>
      <Text>퀴즈 페이지</Text>
      {/* 퀴즈 컴포넌트 */}
    </View>
  );
};

export default QuizPage;
